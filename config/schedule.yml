# https://github.com/ondrejbartas/sidekiq-cron
# use https://crontab.guru/ to validate
# validations for this file exist in /spec/configs/schedule_spec.rb

# DISABLED: this pings hub.2.chatwoot.com daily, which reports plan 'community'
# for this unlicensed self-hosted install. That overwrites
# InstallationConfig['INSTALLATION_PRICING_PLAN'] back to 'community' and triggers
# Internal::ReconcilePlanConfigService, which disables premium features
# (sla, custom_roles, audit_logs, disable_branding, captain_integration,
# csat_review_notes, conversation_required_attributes) on every account.
# executed daily at 0000 UTC
# schedules daily deferred jobs at stable times for each installation
# internal_check_new_versions_job:
#   cron: '0 0 * * *'
#   class: 'Internal::TriggerDailyScheduledItemsJob'
#   queue: scheduled_jobs
# # executed At every 5th minute..
trigger_scheduled_items_job:
  cron: '*/5 * * * *'
  class: 'TriggerScheduledItemsJob'
  queue: scheduled_jobs

# executed hourly for scheduled jobs that do not need minute-level cadence
trigger_hourly_scheduled_items_job:
  cron: '0 * * * *'
  class: 'Internal::TriggerHourlyScheduledItemsJob'
  queue: scheduled_jobs

# executed At every minute..
trigger_imap_email_inboxes_job:
  cron: '*/1 * * * *'
  class: 'Inboxes::FetchImapEmailInboxesJob'
  queue: scheduled_jobs

# executed daily at 2230 UTC
# which is our lowest traffic time
remove_stale_contact_inboxes_job.rb:
  cron: '30 22 * * *'
  class: 'Internal::RemoveStaleContactInboxesJob'
  queue: scheduled_jobs

# executed daily at 2230 UTC
# which is our lowest traffic time
remove_stale_redis_keys_job.rb:
  cron: '30 22 * * *'
  class: 'Internal::RemoveStaleRedisKeysJob'
  queue: scheduled_jobs

# DISABLED: investigating if this job is the source of orphan conversations
# #executed daily at 0430 UTC
# # which will be IST 10:00 AM
# process_stale_contacts_job:
#   cron: '30 04 * * *'
#   class: 'Internal::ProcessStaleContactsJob'
#   queue: housekeeping

# executed daily at 0100 UTC
# to delete accounts marked for deletion
delete_accounts_job:
  cron: '0 1 * * *'
  class: 'Internal::DeleteAccountsJob'
  queue: scheduled_jobs

# executed every 30 minutes for assignment_v2
periodic_assignment_job:
  cron: '*/30 * * * *'
  class: 'AutoAssignment::PeriodicAssignmentJob'
  queue: scheduled_jobs

# executed daily at 2230 UTC
# removes old notifications (>1 month) and trims to 300 per user
remove_old_notification_job:
  cron: '30 22 * * *'
  class: 'Notification::RemoveOldNotificationJob'
  queue: purgable

# executed every 12 hours
# to remove orphan conversations without contact
remove_orphan_conversations_job:
  cron: '0 */12 * * *'
  class: 'Internal::RemoveOrphanConversationsJob'
  queue: housekeeping

# executed every 30 minutes
# sends WhatsApp reactivation messages before Meta's 24h window closes
agent_bot_reactivation_job:
  cron: '*/30 * * * *'
  class: 'AgentBots::ReactivationJob'
  queue: scheduled_jobs

# executed every 30 minutes
# safety net that retries cadence steps/response-checks whose scheduled job was lost
cadences_watchdog_job:
  cron: '*/30 * * * *'
  class: 'Cadences::WatchdogJob'
  queue: scheduled_jobs

# executed hourly
# reactivates "cold" enrollments (exhausted all steps that existed at the time without a
# response) when a new step gets added to their cadence after the fact
cadences_cold_reactivation_job:
  cron: '0 * * * *'
  class: 'Cadences::ColdReactivationJob'
  queue: scheduled_jobs

# executed hourly
# cachea localmente la etapa del Deal de Zoho vinculado a cada contacto convertido, para que el
# reporte de embudo de ventas (V2::Reports::SalesFunnelBuilder) no dependa de consultar Zoho
# en vivo. Solo lectura sobre Zoho.
zoho_deals_sync_job:
  cron: '0 * * * *'
  class: 'Crm::Zoho::DealsSyncJob'
  queue: scheduled_jobs

# executed hourly
# reencola CallAnalysis en failed con intentos disponibles (ver CallAnalysis::RetryFailedAnalysesJob)
call_analysis_retry_failed_job:
  cron: '30 * * * *'
  class: 'CallAnalysis::RetryFailedAnalysesJob'
  queue: scheduled_jobs

# executed every monday at 0600 UTC
# genera el reporte semanal operativo (KPIs + análisis LLM) de la semana recién cerrada, por
# cada inbox con una cadencia activa
generate_weekly_ops_report_job:
  cron: '0 6 * * 1'
  class: 'Reports::GenerateWeeklyOpsReportJob'
  queue: scheduled_jobs

# executed daily at 2235 UTC
# removes raw WhatsApp webhook audit rows older than 30 days
remove_old_whatsapp_webhook_events_job:
  cron: '35 22 * * *'
  class: 'Internal::RemoveOldWhatsappWebhookEventsJob'
  queue: purgable

# executed daily at 0700 UTC
# compares Chatwoot's WhatsApp campaign delivery stats against Meta's template_analytics API
reconcile_meta_analytics_job:
  cron: '0 7 * * *'
  class: 'Campaigns::ReconcileMetaAnalyticsJob'
  queue: scheduled_jobs

# executed hourly at :00 — sincroniza Zoho Leads -> revenue_leads (Revenue Intelligence, Fase 1)
revenue_intelligence_sync_leads_job:
  cron: '0 * * * *'
  class: 'RevenueIntelligence::SyncZohoLeadsJob'
  queue: scheduled_jobs

# executed hourly at :05 — sincroniza Zoho Deals -> revenue_deals (Revenue Intelligence, Fase 1)
revenue_intelligence_sync_deals_job:
  cron: '5 * * * *'
  class: 'RevenueIntelligence::SyncZohoDealsJob'
  queue: scheduled_jobs

# executed hourly at :15 — sincroniza Deals/{id}/Stage_History -> revenue_stage_events, después de
# que Leads/Deals ya corrieron esta hora (Revenue Intelligence, Fase 1)
revenue_intelligence_sync_stage_history_job:
  cron: '15 * * * *'
  class: 'RevenueIntelligence::SyncZohoStageHistoryJob'
  queue: scheduled_jobs

# executed hourly at :20 — sincroniza Zoho Events (Meetings) -> revenue_appointments (Revenue
# Intelligence, Fase 1)
revenue_intelligence_sync_meetings_job:
  cron: '20 * * * *'
  class: 'RevenueIntelligence::SyncZohoMeetingsJob'
  queue: scheduled_jobs

# executed hourly at :25 — resuelve revenue_contact_id de todo lo sincronizado esta hora, corre al
# final a propósito (Revenue Intelligence, Fase 1)
revenue_intelligence_resolve_identity_job:
  cron: '25 * * * *'
  class: 'RevenueIntelligence::ResolveIdentityJob'
  queue: scheduled_jobs

# executed hourly at :30 — construye revenue_events a partir de mensajes/llamadas/análisis/
# leads/deals/stage_events/citas, después de que Fase 1 ya sincronizó esta hora (Revenue
# Intelligence, Fase 2)
revenue_intelligence_build_events_job:
  cron: '30 * * * *'
  class: 'RevenueIntelligence::BuildEventsJob'
  queue: scheduled_jobs

# executed hourly at :40 — agrega revenue_events en revenue_lead_journeys, después de que los
# eventos de esta hora ya existen (Revenue Intelligence, Fase 2)
revenue_intelligence_build_journeys_job:
  cron: '40 * * * *'
  class: 'RevenueIntelligence::BuildJourneysJob'
  queue: scheduled_jobs

# executed hourly at :45 — aplana call_analyses completados en revenue_call_features (Revenue
# Intelligence, Fase 3)
revenue_intelligence_extract_call_features_job:
  cron: '45 * * * *'
  class: 'RevenueIntelligence::ExtractCallFeaturesJob'
  queue: scheduled_jobs

# executed hourly at :50 — agrega revenue_events/revenue_call_features/revenue_stage_events en
# revenue_rollups, después de que las features de esta hora ya existen (Revenue Intelligence,
# Fase 3)
revenue_intelligence_refresh_aggregates_job:
  cron: '50 * * * *'
  class: 'RevenueIntelligence::RefreshAggregatesJob'
  queue: scheduled_jobs

# executed daily at 0610 UTC — reglas determinísticas de riesgo de negocio (deals estancados,
# leads sin contactar, citas sin visita verificada) sobre lo ya sincronizado/agregado (Revenue
# Intelligence, Fase 4). Diario, no horario: son señales de reconciliación periódica, no
# sincronización incremental en caliente.
revenue_intelligence_detect_risks_job:
  cron: '10 6 * * *'
  class: 'RevenueIntelligence::DetectRisksJob'
  queue: scheduled_jobs

# executed daily at 0620 UTC — reglas determinísticas de integridad del propio CRM/sincronización
# (inconsistencias won/lost vs. stage, deal sin lead vinculado, desfase de sync, conflictos de
# identidad sin resolver) (Revenue Intelligence, Fase 4)
revenue_intelligence_detect_data_quality_job:
  cron: '20 6 * * *'
  class: 'RevenueIntelligence::DetectDataQualityIssuesJob'
  queue: scheduled_jobs
